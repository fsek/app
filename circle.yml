general:
  branches:
    only:
      - master
      - production

test:
  override:
    - echo "No tests yet :("

deployment:
  master:
    branch: master
    commands:
      - ./deploy/master
  production:
    branch: production
    commands:
      - ./deploy/production
