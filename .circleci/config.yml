version: 2
jobs:
  build_master:
    machine:
      enabled: true
    steps:
      - checkout
      - run: ./deploy/master
  build_production:
    machine:
      enabled: true
    steps:
      - checkout
      - run: ./deploy/production

workflows:
  version: 2
  build:
    jobs:
      - build_master:
          filters:
              branches:
                only: master
      - build_production:
          filters:
            branches:
              only: production
